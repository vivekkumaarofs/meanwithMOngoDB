import { TransformLanguagePipe } from './transform-language.pipe';

describe('TransformLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
